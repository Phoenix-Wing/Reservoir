{
  "private": true,
  "version": "1.3.0",
  "scripts": {
    "build": "npm run build:queryBuilder && nuxt build",
    "build:query": "pnpm dlx @edgedb/generate queries",
    "clean": "nuxi clean && rm -rf queries/*.query.ts reservoir.tar.gz",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "npm run build:queryBuilder && nuxt preview",
    "postinstall": "nuxt prepare && npm run build:queryBuilder"
  },
  "devDependencies": {
    "@edgedb/generate": "^0.0.8",
    "@huntersofbook/naive-ui-nuxt": "^1.1.0",
    "edgedb": "1.2.1",
    "nuxt": "^3.6.5"
  }
}
