{
  "private": true,
  "scripts": {
    "build": "npm run build:queryBuilder && nuxt build",
    "build:queryBuilder": "rm -rf dbschema/edgeql-js/ && echo n | npx @edgedb/generate edgeql-js --target esm",
    "clean": "rm -rf .output/ .nuxt/ dbschema/edgeql-js/ reservoir.tar.gz",
    "dev": "nuxt dev",
    "dev:queryBuilder": "rm -rf dbschema/edgeql-js/ && echo n | npx @edgedb/generate edgeql-js --target ts",
    "generate": "nuxt generate",
    "preview": "npm run build:queryBuilder && nuxt preview",
    "postinstall": "nuxt prepare && npm run dev:queryBuilder"
  },
  "devDependencies": {
    "@edgedb/generate": "^0.0.8",
    "@huntersofbook/naive-ui-nuxt": "^0.7.1",
    "edgedb": "^1.0.2",
    "nuxt": "3.2.3"
  }
}
