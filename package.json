{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "generate:queryBuilder": "echo n | npx @edgedb/generate edgeql-js --target esm",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && npm run generate:queryBuilder"
  },
  "devDependencies": {
    "@edgedb/generate": "^0.0.8",
    "@huntersofbook/naive-ui-nuxt": "^0.7.1",
    "edgedb": "^1.0.2",
    "nuxt": "3.2.3"
  }
}
