{
  "private": true,
  "scripts": {
    "build": "npm run generate:queryBuilder:release && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "generate:queryBuilder": "rm -rf dbschema/edgeql-js/ && echo n | npx @edgedb/generate edgeql-js --target ts",
    "generate:queryBuilder:release": "rm -rf dbschema/edgeql-js/ && echo n | npx @edgedb/generate edgeql-js --target esm",
    "preview": "npm run generate:queryBuilder:release && nuxt preview",
    "postinstall": "nuxt prepare && npm run generate:queryBuilder"
  },
  "devDependencies": {
    "@edgedb/generate": "^0.0.8",
    "@huntersofbook/naive-ui-nuxt": "^0.7.1",
    "edgedb": "^1.0.2",
    "nuxt": "3.2.3"
  }
}
